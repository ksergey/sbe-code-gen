{% macro define_enum(type, schema) %}
  {% set enum_class_name = type.name | format_enum_name %}
class {{ enum_class_name }}(Enum):
  {% for valid_value in type.valid_values %}
    {% if type.encoding_type.name == 'char' %}
    {{ valid_value.name | format_constant_name }} = '{{ valid_value.value }}'
    {% else %}
    {{ valid_value.name | format_constant_name }} = {{ valid_value.value }}
    {% endif %}
  {% endfor %}
{% endmacro %}

{% macro define_set(type, schema) %}
  {% set set_class_name = type.name | format_set_name %}
class {{ set_class_name }}(Flag):
  {% for choice in type.choices %}
    {{ choice.name | format_constant_name }} = {{ choice.value | bit_to_value}}
  {% endfor %}
{% endmacro %}

{# Is `name` the name of a top-level (schema.types) composite, as opposed to
   an anonymous composite declared inline inside another composite? #}
{% macro is_toplevel_composite(name, schema) -%}
{{ name in (schema.types | selectattr('token', 'equalto', 'composite') | map(attribute='name') | list) }}
{%- endmacro %}

{% macro composite_class_name(member, schema, outer_class_name) -%}
{% if is_toplevel_composite(member.name, schema) == 'True' -%}
{{ member.name | format_composite_name }}
{%- else -%}
{{ outer_class_name }}_{{ member.reference_name | format_composite_name }}
{%- endif %}
{%- endmacro %}

{# ================================================================
   Composite members: pack/unpack a single member of a composite at
   ctx.offset + base_expr + member.offset. base_expr is a *python source
   snippet* ('0' for top-level containers, 'base' inside a reusable
   composite's own pack/unpack).
   ================================================================ #}

{% macro member_pack_stmt(member, value_expr, base_expr, schema, outer_class_name) %}
  {% set addr = base_expr ~ ' + ' ~ member.offset -%}
  {% set bo = '<' if schema.byte_order == 'littleEndian' else '>' -%}
  {% if member.token == 'type' %}
    {% set pt = member.primitive_type -%}
    {% set is_string = pt.name == 'char' -%}
    {% if member.presence == 'required' and member.length == 1 %}
struct.pack_into('{{ bo }}{{ pt.name | struct_fmt }}', ctx.buffer, ctx.offset + {{ addr }}, {{ value_expr }})
    {% elif member.presence == 'optional' and member.length == 1 %}
struct.pack_into('{{ bo }}{{ pt.name | struct_fmt }}', ctx.buffer, ctx.offset + {{ addr }}, {{ value_expr }} if {{ value_expr }} is not None else {{ member.null_value | replace_keyword }})
    {% elif member.presence == 'required' and member.length > 1 and is_string %}
_raw = {{ value_expr }}.encode('utf-8')
if len(_raw) > {{ member.length }}:
    raise ValueError(f'value for "{{ member.reference_name }}" is {len(_raw)} bytes, exceeds field length {{ member.length }}')
if len(_raw) < {{ member.length }}:
    _raw += b'\{{ member.null_value | replace_keyword }}' * ({{ member.length }} - len(_raw))
struct.pack_into('{{ member.length }}s', ctx.buffer, ctx.offset + {{ addr }}, _raw)
    {% elif member.presence == 'required' and member.length > 1 %}
if len({{ value_expr }}) != {{ member.length }}:
    raise ValueError(f'value for "{{ member.reference_name }}" has {len({{ value_expr }})} elements, expected exactly {{ member.length }}')
struct.pack_into('{{ bo }}{{ member.length }}{{ pt.name | struct_fmt }}', ctx.buffer, ctx.offset + {{ addr }}, *{{ value_expr }})
    {% elif member.presence == 'optional' and member.length > 1 and is_string %}
if {{ value_expr }} is not None:
    _raw = {{ value_expr }}.encode('utf-8')
    if len(_raw) > {{ member.length }}:
        raise ValueError(f'value for "{{ member.reference_name }}" is {len(_raw)} bytes, exceeds field length {{ member.length }}')
else:
    _raw = b''
if len(_raw) < {{ member.length }}:
    _raw += b'\{{ member.null_value | replace_keyword }}' * ({{ member.length }} - len(_raw))
struct.pack_into('{{ member.length }}s', ctx.buffer, ctx.offset + {{ addr }}, _raw)
    {% elif member.presence == 'optional' and member.length > 1 %}
if {{ value_expr }} is not None:
    if len({{ value_expr }}) != {{ member.length }}:
        raise ValueError(f'value for "{{ member.reference_name }}" has {len({{ value_expr }})} elements, expected exactly {{ member.length }}')
    struct.pack_into('{{ bo }}{{ member.length }}{{ pt.name | struct_fmt }}', ctx.buffer, ctx.offset + {{ addr }}, *{{ value_expr }})
else:
    struct.pack_into('{{ bo }}{{ pt.name | struct_fmt }}', ctx.buffer, ctx.offset + {{ addr }}, {{ member.null_value | replace_keyword }})
    {% elif member.presence == 'constant' and is_string %}
if {{ value_expr }} is not None and {{ value_expr }} != '{{ member.const_value }}':
    raise ValueError(f'"{{ member.reference_name }}" is a constant field, expected "{{ member.const_value }}" or None, got {{ '{' }}{{ value_expr }}!r{{ '}' }}')
    {% elif member.presence == 'constant' %}
if {{ value_expr }} is not None and {{ value_expr }} != {{ member.const_value | replace_keyword }}:
    raise ValueError(f'"{{ member.reference_name }}" is a constant field, expected {{ member.const_value | replace_keyword }} or None, got {{ '{' }}{{ value_expr }}!r{{ '}' }}')
    {% endif %}
  {% elif member.token == 'enum' %}
    {% set pt = member.encoding_type -%}
    {% if member.presence == 'required' %}
if not isinstance({{ value_expr }}, Enum):
    raise TypeError(f'"{{ member.reference_name }}" expects an Enum member, got {type({{ value_expr }}).__name__}')
struct.pack_into('{{ bo }}{{ pt.name | struct_fmt }}', ctx.buffer, ctx.offset + {{ addr }}, {{ value_expr }}.value{% if pt.name == 'char' %}.encode('utf-8'){% endif %})
    {% elif member.presence == 'optional' %}
if {{ value_expr }} is not None and not isinstance({{ value_expr }}, Enum):
    raise TypeError(f'"{{ member.reference_name }}" expects an Enum member or None, got {type({{ value_expr }}).__name__}')
      {% if pt.name == 'char' %}
struct.pack_into('{{ bo }}c', ctx.buffer, ctx.offset + {{ addr }}, {{ value_expr }}.value.encode('utf-8') if {{ value_expr }} is not None else b'\x00')
      {% else %}
struct.pack_into('{{ bo }}{{ pt.name | struct_fmt }}', ctx.buffer, ctx.offset + {{ addr }}, {{ value_expr }}.value if {{ value_expr }} is not None else {{ member.null_value | replace_keyword }})
      {% endif %}
    {% elif member.presence == 'constant' %}
      {% set enum_name = member.value_ref.split('.')[0] -%}
      {% set enum_value = member.value_ref.split('.')[1] -%}
if {{ value_expr }} is not None and {{ value_expr }} != {{ enum_name | format_enum_name }}.{{ enum_value | format_constant_name }}:
    raise ValueError(f'"{{ member.reference_name }}" is a constant field, expected {{ enum_name | format_enum_name }}.{{ enum_value | format_constant_name }} or None, got {{ '{' }}{{ value_expr }}!r{{ '}' }}')
    {% endif %}
  {% elif member.token == 'set' %}
    {% set pt = member.encoding_type -%}
if not isinstance({{ value_expr }}, Flag):
    raise TypeError(f'"{{ member.reference_name }}" expects a Flag member, got {type({{ value_expr }}).__name__}')
struct.pack_into('{{ bo }}{{ pt.name | struct_fmt }}', ctx.buffer, ctx.offset + {{ addr }}, {{ value_expr }}.value)
  {% elif member.token == 'composite' %}
    {% set nested = composite_class_name(member, schema, outer_class_name) -%}
    {% set first = member.contained_types[0] -%}
    {% if member.presence == 'optional' %}
if {{ value_expr }} is None:
    struct.pack_into('{{ bo }}{{ first.primitive_type.name | struct_fmt }}', ctx.buffer, ctx.offset + {{ addr }} + {{ first.offset }}, {{ first.null_value | replace_keyword }})
else:
    {{ nested }}.pack(ctx, {{ value_expr }}, base={{ addr }})
    {% else %}
{{ nested }}.pack(ctx, {{ value_expr }}, base={{ addr }})
    {% endif %}
  {% endif %}
{% endmacro %}

{% macro member_unpack_stmt(member, base_expr, schema, outer_class_name) %}
  {% set addr = base_expr ~ ' + ' ~ member.offset -%}
  {% set bo = '<' if schema.byte_order == 'littleEndian' else '>' -%}
  {% if member.token == 'type' %}
    {% set pt = member.primitive_type -%}
    {% set is_string = pt.name == 'char' -%}
    {% if member.presence == 'required' and member.length == 1 %}
_v_{{ member.reference_name }} = struct.unpack_from('{{ bo }}{{ pt.name | struct_fmt }}', ctx.buffer, ctx.offset + {{ addr }})[0]
    {% elif member.presence == 'optional' and member.length == 1 %}
_raw = struct.unpack_from('{{ bo }}{{ pt.name | struct_fmt }}', ctx.buffer, ctx.offset + {{ addr }})[0]
      {% if pt.name in ('float', 'double') %}
_v_{{ member.reference_name }} = None if _raw != _raw else _raw
      {% else %}
_v_{{ member.reference_name }} = None if _raw == {{ member.null_value | replace_keyword }} else _raw
      {% endif %}
    {% elif member.presence == 'required' and member.length > 1 and is_string %}
_raw = struct.unpack_from('{{ member.length }}s', ctx.buffer, ctx.offset + {{ addr }})[0]
_v_{{ member.reference_name }} = _raw.split(b'\{{ member.null_value | replace_keyword }}')[0].decode('utf-8')
    {% elif member.presence == 'required' and member.length > 1 %}
_v_{{ member.reference_name }} = list(struct.unpack_from('{{ bo }}{{ member.length }}{{ pt.name | struct_fmt }}', ctx.buffer, ctx.offset + {{ addr }}))
    {% elif member.presence == 'optional' and member.length > 1 and is_string %}
_raw = struct.unpack_from('{{ member.length }}s', ctx.buffer, ctx.offset + {{ addr }})[0]
if _raw[0] == {{ member.null_value | replace_keyword }}:
    _v_{{ member.reference_name }} = None
else:
    _v_{{ member.reference_name }} = _raw.decode('utf-8').rstrip(chr({{ member.null_value | replace_keyword }}))
    {% elif member.presence == 'optional' and member.length > 1 %}
_raw = struct.unpack_from('{{ bo }}{{ member.length }}{{ pt.name | struct_fmt }}', ctx.buffer, ctx.offset + {{ addr }})
_v_{{ member.reference_name }} = list(_raw) if _raw[0] != {{ member.null_value | replace_keyword }} else None
    {% elif member.presence == 'constant' and is_string %}
_v_{{ member.reference_name }} = '{{ member.const_value }}'
    {% elif member.presence == 'constant' %}
_v_{{ member.reference_name }} = {{ member.const_value | replace_keyword }}
    {% endif %}
  {% elif member.token == 'enum' %}
    {% set pt = member.encoding_type -%}
    {% if member.presence == 'required' %}
_v_{{ member.reference_name }} = {{ member.name | format_enum_name }}(struct.unpack_from('{{ bo }}{{ pt.name | struct_fmt }}', ctx.buffer, ctx.offset + {{ addr }})[0]{% if pt.name == 'char' %}.decode('utf-8'){% endif %})
    {% elif member.presence == 'optional' %}
_raw = struct.unpack_from('{{ bo }}{{ pt.name | struct_fmt }}', ctx.buffer, ctx.offset + {{ addr }})[0]
      {% if pt.name == 'char' %}
_v_{{ member.reference_name }} = None if _raw == b'\x00' else {{ member.name | format_enum_name }}(_raw.decode('utf-8'))
      {% else %}
_v_{{ member.reference_name }} = None if _raw == {{ member.null_value | replace_keyword }} else {{ member.name | format_enum_name }}(_raw)
      {% endif %}
    {% elif member.presence == 'constant' %}
      {% set enum_name = member.value_ref.split('.')[0] -%}
      {% set enum_value = member.value_ref.split('.')[1] -%}
_v_{{ member.reference_name }} = {{ enum_name | format_enum_name }}.{{ enum_value | format_constant_name }}
    {% endif %}
  {% elif member.token == 'set' %}
    {% set pt = member.encoding_type -%}
_v_{{ member.reference_name }} = {{ member.name | format_set_name }}(struct.unpack_from('{{ bo }}{{ pt.name | struct_fmt }}', ctx.buffer, ctx.offset + {{ addr }})[0])
  {% elif member.token == 'composite' %}
    {% set nested = composite_class_name(member, schema, outer_class_name) -%}
    {% set first = member.contained_types[0] -%}
    {% if member.presence == 'optional' %}
_raw = struct.unpack_from('{{ bo }}{{ first.primitive_type.name | struct_fmt }}', ctx.buffer, ctx.offset + {{ addr }} + {{ first.offset }})[0]
if _raw == {{ first.null_value | replace_keyword }}:
    _v_{{ member.reference_name }} = None
else:
    _v_{{ member.reference_name }} = {{ nested }}.unpack(ctx, base={{ addr }})
    {% else %}
_v_{{ member.reference_name }} = {{ nested }}.unpack(ctx, base={{ addr }})
    {% endif %}
  {% endif %}
{% endmacro %}

{% macro member_field_decl(member, schema, outer_class_name) %}
  {% if member.token == 'composite' %}
    {% set hint = composite_class_name(member, schema, outer_class_name) -%}
  {% elif member.token == 'enum' %}
    {% set hint = member.name | format_enum_name -%}
  {% elif member.token == 'set' %}
    {% set hint = member.name | format_set_name -%}
  {% elif member.presence == 'constant' and member.token == 'type' and member.primitive_type.name == 'char' %}
    {% set hint = 'str' -%}
  {% elif member.length > 1 and member.primitive_type.name == 'char' %}
    {% set hint = 'str' -%}
  {% elif member.length > 1 %}
    {% set hint = 'List[' ~ ('float' if member.primitive_type.name in ('float', 'double') else 'int') ~ ']' -%}
  {% elif member.primitive_type.name == 'char' %}
    {% set hint = 'bytes' -%}
  {% else %}
    {% set hint = 'float' if member.primitive_type.name in ('float', 'double') else 'int' -%}
  {% endif %}
  {% if member.presence == 'optional' %}
{{ member.reference_name }}: Optional[{{ hint }}] = None
  {% elif member.presence == 'constant' %}
    {% if member.token == 'enum' %}
      {% set enum_name = member.value_ref.split('.')[0] -%}
      {% set enum_value = member.value_ref.split('.')[1] -%}
{{ member.reference_name }}: {{ hint }} = {{ enum_name | format_enum_name }}.{{ enum_value | format_constant_name }}
    {% elif member.token == 'type' and member.primitive_type.name == 'char' %}
{{ member.reference_name }}: {{ hint }} = '{{ member.const_value }}'
    {% else %}
{{ member.reference_name }}: {{ hint }} = {{ member.const_value | replace_keyword }}
    {% endif %}
  {% else %}
{{ member.reference_name }}: {{ hint }}
  {% endif %}
{% endmacro %}

{# ================================================================
   A named composite (top-level, or anonymous/nested inside another).
   Its pack()/unpack() take an explicit `base` offset so the SAME class
   can be embedded at different byte offsets in different messages,
   groups or other composites, instead of regenerating its packing
   logic inline at every use site.
   ================================================================ #}

{% macro define_composite(type, schema, class_name=none) %}
  {% set class_name = class_name if class_name else (type.name | format_composite_name) -%}
  {% for member in type.contained_types if member.token == 'composite' and is_toplevel_composite(member.name, schema) != 'True' %}
{{ define_composite(member, schema, class_name ~ '_' ~ (member.reference_name | format_composite_name)) }}
  {% endfor %}

@dataclass(kw_only=True)
class {{ class_name }}:
  {% for member in type.contained_types %}
    {{ member_field_decl(member, schema, class_name).strip() | indent(4) }}
  {% endfor %}

    ENCODED_LENGTH: ClassVar[int] = {{ type.encoded_length }}

    @staticmethod
    def pack(ctx: CodecContext, value: '{{ class_name }}', base: int = 0) -> None:
        ctx.reserve(base + {{ type.encoded_length }})
  {% for member in type.contained_types %}
        {{ member_pack_stmt(member, 'value.' ~ member.reference_name, 'base', schema, class_name).strip() | indent(8) }}
  {% endfor %}

    @staticmethod
    def unpack(ctx: CodecContext, base: int = 0) -> '{{ class_name }}':
  {% for member in type.contained_types %}
        {{ member_unpack_stmt(member, 'base', schema, class_name).strip() | indent(8) }}
  {% endfor %}
        return {{ class_name }}(
  {% for member in type.contained_types %}
            {{ member.reference_name }}=_v_{{ member.reference_name }},
  {% endfor %}
        )
{% endmacro %}

{# ================================================================
   Variable-length data fields.
   ================================================================ #}

{% macro data_field_decl(field, schema) %}
  {% set var_data = (field.type.contained_types | selectattr('name', 'equalto', 'varData') | first) -%}
  {% set is_string = var_data.character_encoding and var_data.primitive_type.size == 1 -%}
  {% if is_string %}
{{ field.name }}: str = ''
  {% else %}
{{ field.name }}: List[int] = dataclasses_field(default_factory=list)
  {% endif %}
{% endmacro %}

{% macro data_pack_stmt(field, value_expr, schema) %}
  {% set length = (field.type.contained_types | selectattr('name', 'equalto', 'length') | first) -%}
  {% set var_data = (field.type.contained_types | selectattr('name', 'equalto', 'varData') | first) -%}
  {% set is_string = var_data.character_encoding and var_data.primitive_type.size == 1 -%}
  {% set bo = '<' if schema.byte_order == 'littleEndian' else '>' -%}
  {% if is_string %}
_raw = {{ value_expr }}.encode('{{ var_data.character_encoding }}')
if len(_raw) > {{ length.max_value | replace_keyword }}:
    raise ValueError(f'value for "{{ field.name }}" is {len(_raw)} bytes, exceeds the maximum {{ length.max_value | replace_keyword }}')
ctx.reserve({{ length.encoded_length }} + len(_raw))
struct.pack_into('{{ bo }}{{ length.primitive_type.name | struct_fmt }}', ctx.buffer, ctx.offset, len(_raw))
ctx.offset += {{ length.encoded_length }}
struct.pack_into(f'{len(_raw)}s', ctx.buffer, ctx.offset, _raw)
ctx.offset += len(_raw)
  {% else %}
if len({{ value_expr }}) > {{ length.max_value | replace_keyword }}:
    raise ValueError(f'value for "{{ field.name }}" has {len({{ value_expr }})} elements, exceeds the maximum {{ length.max_value | replace_keyword }}')
ctx.reserve({{ length.encoded_length }} + len({{ value_expr }}))
struct.pack_into('{{ bo }}{{ length.primitive_type.name | struct_fmt }}', ctx.buffer, ctx.offset, len({{ value_expr }}))
ctx.offset += {{ length.encoded_length }}
struct.pack_into(f'{{ bo }}{len({{ value_expr }})}{{ var_data.primitive_type.name | struct_fmt }}', ctx.buffer, ctx.offset, *{{ value_expr }})
ctx.offset += len({{ value_expr }})
  {% endif %}
{% endmacro %}

{% macro data_unpack_stmt(field, schema) %}
  {% set length = (field.type.contained_types | selectattr('name', 'equalto', 'length') | first) -%}
  {% set var_data = (field.type.contained_types | selectattr('name', 'equalto', 'varData') | first) -%}
  {% set is_string = var_data.character_encoding and var_data.primitive_type.size == 1 -%}
  {% set bo = '<' if schema.byte_order == 'littleEndian' else '>' -%}
_length = struct.unpack_from('{{ bo }}{{ length.primitive_type.name | struct_fmt }}', ctx.buffer, ctx.offset)[0]
ctx.offset += {{ length.encoded_length }}
  {% if is_string %}
_v_{{ field.name }} = struct.unpack_from(f'{_length}s', ctx.buffer, ctx.offset)[0].decode('{{ var_data.character_encoding }}')
  {% else %}
_v_{{ field.name }} = list(struct.unpack_from(f'{{ bo }}{_length}{{ var_data.primitive_type.name | struct_fmt }}', ctx.buffer, ctx.offset))
  {% endif %}
ctx.offset += _length
{% endmacro %}

{# ================================================================
   Groups: entries are their own dataclass (recursively, so a group can
   itself contain nested groups and/or data fields). A group entry is
   never embedded anywhere else the way a composite is, so base is
   always implicitly 0 for its own fields.
   ================================================================ #}

{% macro group_entry_class_name(field, outer_class_name) -%}
{{ outer_class_name }}_{{ field.name | format_composite_name }}
{%- endmacro %}

{% macro dimension_type_class_name(field) -%}
{{ field.dimension_type.name | format_composite_name }}
{%- endmacro %}

{% macro container_field_decls(fields, schema, outer_class_name) %}
  {% for field in fields if field.token == 'field' %}
{{ member_field_decl(field | as_member, schema, outer_class_name).strip() }}
  {% endfor %}
  {% for field in fields if field.token == 'group' %}
{{ field.name }}: List['{{ group_entry_class_name(field, outer_class_name) }}'] = dataclasses_field(default_factory=list)
  {% endfor %}
  {% for field in fields if field.token == 'data' %}
{{ data_field_decl(field, schema).strip() }}
  {% endfor %}
{% endmacro %}

{% macro container_pack_fixed_fields(fields, schema, outer_class_name) %}
  {% for field in fields if field.token == 'field' %}
{{ member_pack_stmt(field | as_member, 'value.' ~ field.name, '0', schema, outer_class_name).strip() }}
  {% endfor %}
{% endmacro %}

{% macro container_pack_variable_parts(fields, schema, outer_class_name) %}
  {% for field in fields if field.token == 'group' %}
_entries = value.{{ field.name }}
if not isinstance(_entries, (list, tuple)):
    raise TypeError(f'group "{{ field.name }}" expects a list, got {type(_entries).__name__}')
{{ dimension_type_class_name(field) }}.pack(ctx, {{ dimension_type_class_name(field) }}(blockLength={{ field.block_length }}, numInGroup=len(_entries)))
ctx.offset += {{ field.dimension_type.encoded_length }}
for _entry in _entries:
    {{ group_entry_class_name(field, outer_class_name) }}.pack(ctx, _entry)
  {% endfor %}
  {% for field in fields if field.token == 'data' %}
{{ data_pack_stmt(field, 'value.' ~ field.name, schema).strip() }}
  {% endfor %}
{% endmacro %}

{% macro container_unpack_fixed_fields(fields, schema, outer_class_name) %}
  {% for field in fields if field.token == 'field' %}
{{ member_unpack_stmt(field | as_member, '0', schema, outer_class_name).strip() }}
  {% endfor %}
{% endmacro %}

{% macro container_unpack_variable_parts(fields, schema, outer_class_name) %}
  {% for field in fields if field.token == 'group' %}
_dim = {{ dimension_type_class_name(field) }}.unpack(ctx)
ctx.offset += {{ field.dimension_type.encoded_length }}
_v_{{ field.name }} = [{{ group_entry_class_name(field, outer_class_name) }}.unpack(ctx, _dim.blockLength) for _ in range(_dim.numInGroup)]
  {% endfor %}
  {% for field in fields if field.token == 'data' %}
{{ data_unpack_stmt(field, schema).strip() }}
  {% endfor %}
{% endmacro %}

{% macro define_group_entry(field, schema, outer_class_name) %}
  {% set class_name = group_entry_class_name(field, outer_class_name) -%}
  {% for subfield in field.fields if subfield.token == 'group' %}
{{ define_group_entry(subfield, schema, class_name) }}
  {% endfor %}

@dataclass(kw_only=True)
class {{ class_name }}:
    {{ container_field_decls(field.fields, schema, class_name).strip() | indent(4) if field.fields else 'pass' }}

    BLOCK_LENGTH: ClassVar[int] = {{ field.block_length }}

    @staticmethod
    def pack(ctx: CodecContext, value: '{{ class_name }}') -> None:
        ctx.reserve({{ field.block_length }})
        {{ container_pack_fixed_fields(field.fields, schema, class_name).strip() | indent(8) }}
        ctx.offset += {{ field.block_length }}
        {{ container_pack_variable_parts(field.fields, schema, class_name).strip() | indent(8) }}

    @staticmethod
    def unpack(ctx: CodecContext, acting_block_length: int = {{ field.block_length }}) -> '{{ class_name }}':
        if acting_block_length < {{ field.block_length }}:
            raise ValueError(f'acting_block_length {acting_block_length} is smaller than {{ class_name }}.BLOCK_LENGTH ({{ field.block_length }})')
        {{ container_unpack_fixed_fields(field.fields, schema, class_name).strip() | indent(8) }}
        ctx.offset += acting_block_length
        {{ container_unpack_variable_parts(field.fields, schema, class_name).strip() | indent(8) }}
        return {{ class_name }}(
  {% for subfield in field.fields %}
            {{ subfield.name }}=_v_{{ subfield.name }},
  {% endfor %}
        )
{% endmacro %}

{# ================================================================
   A message: same container shape as a group entry (fixed fields, then
   groups, then data), plus TEMPLATE_ID/BLOCK_LENGTH classvars.
   ================================================================ #}

{% macro define_message(message, schema) %}
  {% set class_name = message.name | format_message_name -%}
  {% for field in message.fields if field.token == 'group' %}
{{ define_group_entry(field, schema, class_name) }}
  {% endfor %}

@dataclass(kw_only=True)
class {{ class_name }}:
    {{ container_field_decls(message.fields, schema, class_name).strip() | indent(4) if message.fields else 'pass' }}

    TEMPLATE_ID: ClassVar[int] = {{ message.id }}
    BLOCK_LENGTH: ClassVar[int] = {{ message.block_length }}

    @staticmethod
    def pack(ctx: CodecContext, value: '{{ class_name }}') -> None:
        ctx.reserve({{ message.block_length }})
        {{ container_pack_fixed_fields(message.fields, schema, class_name).strip() | indent(8) }}
        ctx.offset += {{ message.block_length }}
        {{ container_pack_variable_parts(message.fields, schema, class_name).strip() | indent(8) }}

    @staticmethod
    def unpack(ctx: CodecContext, acting_block_length: int = {{ message.block_length }}) -> '{{ class_name }}':
        if acting_block_length < {{ message.block_length }}:
            raise ValueError(f'acting_block_length {acting_block_length} is smaller than {{ class_name }}.BLOCK_LENGTH ({{ message.block_length }})')
        {{ container_unpack_fixed_fields(message.fields, schema, class_name).strip() | indent(8) }}
        ctx.offset += acting_block_length
        {{ container_unpack_variable_parts(message.fields, schema, class_name).strip() | indent(8) }}
        return {{ class_name }}(
  {% for field in message.fields %}
            {{ field.name }}=_v_{{ field.name }},
  {% endfor %}
        )
{% endmacro %}
